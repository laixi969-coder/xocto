__pycache__/
*.py[cod]
.venv/
.DS_Store

# rebuild 产生的旧产品池备份，确认无误后可以自己删
data/pool.bak-*/

# 注意：data/raw/ 进库。
# 云端每天自动采集时容器是全新的，不提交的话当天的原始存档就永久丢了，
# 而它是"解析规则改进后重建历史"的唯一依据。
# 体积靠保留期控制：`xocto prune` 只留最近 30 天，约 30MB。

# 下面这些要进库 —— 网站靠它们生成，也是这个项目的真正资产：
#   data/pool/       产品档案
#   data/analysis/   深度分析
#   data/reports/    每日观察
#   site/            生成好的静态站（部署用，也方便直接在 GitHub 上看）
# 项目记忆/会话数据，不进版本库
.workbuddy/

# 只对自己有用的联想和工作笔记。data/ 会全部上站，私人内容只放这里。
.private/

# 24MB+ 中文字体源文件，子集化原料；产物 site/fonts/ 已入库
templates/fonts/*.otf
.gstack/

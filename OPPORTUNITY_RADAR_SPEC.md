# xOcto 全球 AI+ 机会流迁移规格

状态：待实施

本文件固化产品方向与实施边界。它不是营销文案，也不是一次性改版清单；后续数据模型、采集、判断、页面和测试均以此为准。

## 1. 产品定义

xOcto 是一个每日更新的全球 AI+ 早期机会系统。

它记录任何行业、任何地域中开始被 AI 重新产品化的项目与变化，包括：

- 新出现的 AI 应用或服务；
- 开源项目及其采用信号；
- 已有软件、传统业务或行业公司发生的实质性 AI 改造；
- 已在一个市场出现验证、而另一个市场本地供给仍待观察的跨国机会。

首页的核心交付是**今日首次发现的机会流**。它不是热度榜、产品目录或少量精选案例。

产品的行业、具体工作、地区、阶段、信号和跨国关系均为并列维度，不能合并为一个固定标签或单一分类。

## 2. 收录边界

### 2.1 应进入机会流的对象

项目满足以下任一条件即可进入，前提是存在可核验的产品、代码、发布或业务变化：

1. 新发布的 AI 应用、服务或开源项目；
2. 出现用户、收入、采用、维护或生态扩张信号的早期项目；
3. 某个行业的具体工作首次被 AI 产品化；
4. 已有产品的 AI 改造实质改变了工作流、交付结果、付费方式或服务边界；
5. 中文或英文生态的一方已有可验证供给或需求证据，另一方本地供给仍待观察。

### 2.2 不应进入机会流的对象

- 不构成独立产品、开源项目或实质业务变化的新闻转载；
- 成熟产品添加普通聊天、摘要或生成按钮，但未改变工作流、交付结果、付费方式或服务边界的功能更新；
- 无可核验实体的概念页、提示词包装或纯目录站；
- 重复采集到、但没有新增事实的既有项目。

“需求存疑”不是排除条件。项目仍可进入机会流，但必须带有明确的 `/req` 初步判断和证据不足说明。

## 3. 每日事件规则

### 3.1 事件类型

| 事件 | 定义 | 首页位置 |
|---|---|---|
| `first_discovered` | xOcto 首次发现该项目 | 今日首次发现；仅出现一个自然日 |
| `material_update` | 已入库项目出现可核验的新事实，且改变判断或市场状态 | 重要更新；仅说明变化 |
| `market_change` | 项目进入新市场，或中英文市场的本地供给状态发生变化 | 重要更新，并更新跨国对照 |
| `req_change` | 新证据改变 `/req` 任一闸门或结论 | 重要更新，并说明改变的闸门 |

### 3.2 时间

必须同时保存：

- 项目公开发布时间（若可取得）；
- xOcto 首次发现时间；
- 每条事件的发现时间；
- 每条证据的发布时间与抓取时间；
- 每次 `/req` 判断版本的生成时间。

首页按 xOcto 首次发现时间排序，而不是按项目公开时间、热度或访问量排序。项目公开较早但当天首次被发现时，必须同时展示两个时间，避免将“新发现”误写为“新发布”。

### 3.3 去重与更新

- 同一项目次日被重新采集，不产生新首页条目；
- 只有新增用户、收入、定价、客户、使用、开源活跃度、市场进入或判断变化等事实时，创建实质更新；
- 实质更新不重新进入“今日首次发现”，只进入首页次要位置“重要更新”；
- 每个更新必须包含“新增事实”与“受影响的判断”，不得重复产品简介。

## 4. 数据结构

现有单一 `Product` 档案不足以表达每日事件、多个市场和多次判断。目标模型由五类独立记录组成。

### 4.1 Project

表示稳定的项目本体。

必需字段：

- 标识、名称、规范化网址、所属组织或作者；
- 项目类型：`new_application`、`open_source`、`ai_transformation`、`market_context`；
- 产品状态：公开、停更、归档等；
- 简要功能说明与开源状态；
- 原始发布时间（如可验证）。

### 4.2 DiscoveryEvent

表示 xOcto 在某日发现的新增事实。

必需字段：

- 关联项目；
- 事件类型；
- 首次发现或更新发生时间；
- 早期信号标签：发布、采用、收入、开源、行业产品化、跨国机会等；
- 事件摘要；
- 支持事件的 `Evidence` 引用；
- 是否应进入首页及原因。

### 4.3 MarketObservation

表示项目在一个具体市场的可验证状态。中文与英文生态是首页对照层；底层记录仍按国家或市场保存。

必需字段：

- 市场/国家；
- 语言生态；
- 团队或首发市场；
- 产品可访问市场；
- 已验证采用市场；
- 本地供给状态：`not_found_in_covered_sources`、`emerging`、`established`；
- 需求证据状态：`unknown`、`early_signal`、`validated_signal`；
- 覆盖范围、检索日期与证据引用。

“未发现”只能表示“在指定日期、指定公开覆盖范围内未发现可验证的本地供给”，绝不表示绝对不存在。

### 4.4 ReqReview

表示一次基于 `/req` 的判断版本。

必需字段：

- 判断层级：`initial` 或 `full`；
- 价值、共识、模式、求真四道闸门的状态、理由与证据引用；
- 结论：`true_demand`、`pseudo_demand`、`needs_validation`；
- 对外显示的信号等级：需求信号明确、初步成立、待验证、需求存疑；
- 下一项待验证事实或最小验证动作；
- 生成时间与适用市场。

### 4.5 Evidence

表示可核验事实，而不是内部采集策略。

必需字段：

- URL、标题、发布时间、获取时间；
- 证据类型：产品/定价/开源/采用/收入/市场对照/独立报道等；
- 一手事实、行为证据或编辑判断的层级；
- 所支持的项目、事件、市场观察或 `/req` 闸门；
- 支持的具体事实摘录或结构化字段。

详情页展示 Evidence 链接与其支持的判断。内部采集渠道、抓取规则和完整来源策略不对外展示。

## 5. `/req` 编辑版判断

`/req` 是决策闸门，不是热度评分器。其原始四道闸门为价值、共识、模式、求真；xOcto 必须保留这一顺序和“证据不足不补造事实”的原则。

### 5.1 初步判断（首页卡片）

初步判断在公开信息有限时执行。它不能因为无法与创始人对话，就将项目判为伪需求。

| 闸门 | 可公开判断的问题 | 信息不足时的状态 |
|---|---|---|
| 价值 | 用户是谁、原有做法是什么、痛点是否具体、交付是否可验证 | 待验证 |
| 共识 | 是否存在明确场景、采用信号、利益相关者或改变动机 | 待验证 |
| 模式 | 付费者是谁、定价/收入/单位经济证据是否存在 | 待验证 |
| 求真 | 产品是否接近行业第一性、是否存在明显安全或交付边界 | 待验证 |

首页显示结论、最强证据和最重要的未知项；不显示长篇推理。

### 5.2 完整判断（详情页）

详情页按四道闸门展开，并分别显示：

- 已知事实与证据；
- 该闸门的判断；
- 不能确认的部分；
- 下一步必须验证的事实。

`pseudo_demand` 仅适用于存在明确反证，或价值、场景、付费逻辑已明显不成立的项目。对新项目最常见的结论应是 `needs_validation`，而不是强行给出“真需求”或“伪需求”。

## 6. 中英文生态与跨国机会

第一阶段同时覆盖中文与英文生态。底层市场记录按具体国家或市场保存，首页将其汇总为“中文生态 / 英文生态”。

每个项目每天生成对照结论：

| 维度 | 中文生态 | 英文生态 |
|---|---|---|
| 本地供给 | 未发现 / 早期出现 / 已有成熟供给 | 未发现 / 早期出现 / 已有成熟供给 |
| 需求证据 | 待验证 / 初步成立 / 信号明确 | 待验证 / 初步成立 / 信号明确 |
| 证据 | 可核验链接 | 可核验链接 |

若一方已有可验证的产品、采用或需求证据，另一方本地供给仍待观察，项目直接标记为**跨国机会**。

“产品可访问”不等于“本地供给存在”。海外产品即使可被中国用户访问，但没有本地化工作流、数据与集成、支付、合规、销售或交付能力时，仍可判断为“海外已有验证；中国本地供给仍待观察”。

跨国机会是差异信号，不是已经验证的复制结论；本地需求刚性、工作流差异、支付与合规边界仍由 `/req` 判断。

## 7. 信息架构

### 7.1 首页

首页固定为三个层级：

1. **今日首次发现**：当天全部首次发现项目，按发现时间倒序，是页面主体；
2. **重要更新**：已入库项目的新事实与受影响判断，数量少于首次发现；
3. **当日市场摘要**：仅归纳当天出现的跨国、行业或开源变化，不替代项目流。

每条首页卡至少显示：项目名称、项目类型、行业/工作/地区标签、早期信号、`/req` 初判、首次发现时间、公开发布时间（如可得）和详情入口。

### 7.2 项目详情页

详情页固定顺序：

1. 项目与功能边界；
2. 行业、具体工作、地区与项目类型；
3. `/req` 初步或完整判断；
4. 中英文生态对照与跨国机会；
5. 功能、用户、付费者与商业模式；
6. 证据链；
7. 重要更新历史与待验证项。

### 7.3 机会库

所有过期的“今日首次发现”项目次日进入机会库。机会库支持并列筛选：

- 稳定大类；
- 可增长的细分行业与具体工作标签；
- 项目类型；
- 国家/市场与语言生态；
- 早期信号；
- 开源状态；
- 跨国机会；
- `/req` 结论与证据强度；
- 发现日期与实质更新时间。

默认排序为最近首次发现，而不是热度或既有规模。

## 8. 分类体系

采用两层分类：

- 稳定大类：用于长期导航和统计；
- 细分行业与具体工作标签：可新增、可归并，但保留原始标签。

稳定大类不限制机会范围；其作用只是提供可持续的浏览入口。细分标签应优先描述真实工作，而非模型、Agent、RAG 等技术形态。

## 9. 采集与覆盖原则

中文与英文生态分别维护发现链路。两者均应覆盖：

- 新项目与产品发布；
- 开源项目与社区采用；
- 行业原生渠道中的 AI 改造；
- 用户、客户、付费、招聘、集成等行为证据；
- 国家级或语言生态级的本地供给对照。

产品发布平台和开发者社区只能是发现入口之一，不能成为唯一入口；否则机会流会再次偏向通用助手、Agent 基础设施和开发工具。

## 10. 实施顺序

1. 建立 `DiscoveryEvent`、`Evidence`、`MarketObservation`、`ReqReview` 数据模型及历史兼容策略；
2. 改造采集与去重：区分首次发现、重复出现与实质更新；
3. 将 `/req` 接入为编辑版初步判断与详情版完整判断；
4. 建立中英文生态的市场观察与跨国机会对照；
5. 改造首页为当日首次发现流与重要更新；
6. 改造机会库为多维历史检索；
7. 扩展中文和英文的行业原生发现链路，并为每条判断保留证据。

## 11. 验收标准

迁移完成后，系统必须满足：

- 同一项目不会因重复采集连续多日出现在“今日首次发现”；
- 当天全部通过最低实体校验的新项目都进入机会流，并带有 `/req` 初判；
- 每个公开判断均能链接到支持它的事实证据；
- 每个项目均有中文与英文生态的当日对照结论，且“未发现”带覆盖范围与日期；
- 跨国机会与项目来源国、产品可访问性、本地供给状态严格区分；
- 成熟产品的无实质 AI 改造不会占据机会流；
- 次日首页的主体内容与前一日不同，历史项目仅在发生实质更新时出现；
- 机会库能按行业、工作、地区、信号、项目类型、开源状态和 `/req` 结论交叉检索。

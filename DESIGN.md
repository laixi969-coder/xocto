# DESIGN.md

x-octo 的设计系统。纯静态站，单个 `templates/style.css`，无框架无构建。

## 定位：情报终端 × 编辑部

不是产品目录，不是数据看板。是一个有主见的编辑在跟你说话。
纸感的底色、克制的衬线标题、等宽的数字、一个只给信号用的强调色。

**场景句**：一个做商业判断的人，早上第一件事，在 27 寸屏幕上花三分钟扫一遍
昨夜全球冒出来的东西，找一两个值得深看的。明亮的房间，注意力有限，随时会关掉。

→ 这个场景要求**浅色为底**（明亮环境、长文阅读），暗色作为可切换项而非默认。

## 色彩

策略：**Restrained**（克制）—— 偏暖的中性色 + 一个强调色，强调色占比 ≤10%，
只用在两处：增长率数字、推荐评级。其余全部靠中性色的层次。

| 角色 | 浅色 | 暗色 | 用途 |
|------|------|------|------|
| paper | `#fbfaf8` | `#101012` | 页面底色，偏暖的纸白，不用纯白 |
| surface | `#ffffff` | `#18181b` | 需要抬起的表面 |
| ink | `#17171a` | `#ededf0` | 正文，不用纯黑 |
| ink-soft | `#55555f` | `#a3a3ae` | 次级文字 |
| ink-faint | `#8a8a94` | `#6e6e7a` | 标签、元信息 |
| line | `#e6e3dd` | `#2b2b32` | 主分隔线 |
| line-soft | `#f1efeb` | `#212127` | 次级分隔线、hover 底色 |
| **signal** | `#b8410c` | `#f97316` | **唯一强调色**：增长率、推荐评级、灵感边框 |
| good | `#15803d` | `#4ade80` | 「值得关注」评级 |
| watch | `#a16207` | `#fbbf24` | 「有待观察」评级 |

**待改进**：目前用 hex，应迁移到 OKLCH，并让中性色统一偏向 signal 的色相。

## 排版

- **标题**：`"Songti SC", "Noto Serif SC", Georgia, serif` —— 衬线，编辑部气质
- **正文**：系统无衬线栈（PingFang SC 优先）
- **数字**：`ui-monospace, "SF Mono", Menlo` —— 所有数字等宽，表格能对齐

层级靠**字号 + 字重 + 字体族**三者拉开，不靠单一维度：

| 层级 | 字号 | 字体 | 字重 |
|------|------|------|------|
| 页面主标题 | clamp(26px, 3.4vw, 36px) | serif | 700 |
| 区块标题 | 20px | serif | 700 |
| 卡片/条目标题 | 15-19px | sans | 600-700 |
| 正文 | 15px | sans | 400 |
| 元信息/标签 | 11-13px | sans | 400 |

正文行长控制在 62-78ch。

## 间距与布局

- 页面最大宽 1120px
- 区块间距 40px，顶部条 52px
- **刻意不对称**：顶部条是 1.9fr / 1fr，判断条目是 1fr / 2.4fr
- **少用卡片**：扫读的内容用分隔线（`border-bottom` + hover 变底色），
  需要停留的内容才用卡片。禁止嵌套卡片。

## 动效

- 缓动统一 `cubic-bezier(0.16, 1, 0.3, 1)`，200ms
- 只动 `transform` 和 `opacity`
- 载入用 `animation-delay` 级联，每项差 60-70ms
- hover 反馈：列表条目左移 6px + 变底色；卡片上浮 1px + 边框变强调色
- 全局尊重 `prefers-reduced-motion`

## 组件清单

| 类名 | 用途 |
|------|------|
| `.lede-bar` | 顶部条，左右不对称，直接给结论不喊标语 |
| `.pick` | 今日判断条目，左边名字右边内容 |
| `.rows` / `.row-insp` | 密集列表，第二行是灵感（带 signal 左边框） |
| `.tbl` | 数据表，数字等宽右对齐 |
| `.obs-item` | 每日观察卡片，钩子用衬线大字 |
| `.cat` | 赛道入口 |
| `.card` / `.card-proven` | 产品卡片，已验证型重数据、早期型重描述 |
| `.verdict` | 三档评级标签 |
| `.field-take` | 可迁移点，signal 左边框 |

## 明确不要

- 侧边色条（`border-left` 粗色条当装饰）—— 例外：`.row-insp` 和 `.field-take`
  的 2px signal 边框是**语义标记**（这是灵感/可拿走的东西），不是装饰
- 渐变文字、玻璃拟态、三栏等宽卡片行
- emoji、感叹号、em dash
- 大标题喊口号（"每天挖掘全球 AI 应用" 这种）
